{
  "name": "functions",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "./node_modules/.bin/tsc",
    "deploy": "npm run build && gcloud functions deploy upload-to-bigquery --entry-point uploadToBigQuery --runtime nodejs8 --trigger-resource techsylvania_travel_data --trigger-event google.storage.object.finalize --memory 2048MB --timeout 540 --project techsylvania-2019-demo --region europe-west1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/bigquery": "^4.1.0",
    "@google-cloud/storage": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^12.0.4",
    "typescript": "^3.5.1"
  }
}
